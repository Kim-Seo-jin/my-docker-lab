  events {
      worker_connections 1024;
  }

  http {
      server {
          listen 80;

          location / {
              proxy_pass http://tomcat:8080;
              # "tomcat" = docker-compose의 서비스 이름
              # Docker가 자동으로 IP로 변환해줌
          }
      }
  }